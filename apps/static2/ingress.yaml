apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
  namespace: static2
spec:
  ingressClassName: nginx
  rules:
  - host: "echo2.192.168.1.31.nip.io"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: echo-service
            port:
              number: 80
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: echo-service
  name: echo-service
  namespace: static2
spec:
  type: ClusterIP
  ports:
  - name: echo-deployment2
    port: 80
    targetPort: 5678
  selector:
    app: echo-server